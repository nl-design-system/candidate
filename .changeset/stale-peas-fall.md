---
'@nl-design-system-candidate/color-sample-css': minor
'@nl-design-system-candidate/color-sample-tokens': minor
---

Add `--nl-color-sample-value` custom property so the CSS-only variant can render an actual color (with correct transparency blending against the checkerboard) without requiring the React SVG implementation
