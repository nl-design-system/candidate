---
'@nl-design-system-candidate/button-react': patch
---

Keep busy toggle buttons operable by no longer setting `aria-disabled`, so an accidental toggle (e.g. "Like") can still be undone while it's pending
