---
'@nl-design-system-candidate/link-react': patch
---

`aria-current` en `aria-disabled` zijn depricated omdat de `current` en `disabled` props deze attributen zetten en styling aanpassen, en dus gebruikt moeten worden
