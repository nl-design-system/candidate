// Entry script for the Playwright Component Testing mount page (loaded by ./index.html).
// Playwright rewrites this file at test-run time to render the component under test into
// `#root`, so it's required infrastructure even though it's empty here, edit it only to
// add global setup (e.g. a theme provider or stylesheet) shared by every mounted component.
// See https://playwright.dev/docs/test-components#how-it-works
export {};
